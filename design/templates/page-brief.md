# Page brief: [name]

Bind to `/design-plan.md`. Do not introduce new type, color, or motion.

## Intent

- Route:
- Dominant intention (one):
- Intensity: `quiet` | `expressive` | `immersive`
- Primary action:
- Secondary action (optional, one):

## Information sequence

1. Orientation:
2. Value or claim:
3. Proof or context:
4. Action:
5. Deeper detail:

## Rupture on this page

- Word- or sentence-as-object:
- 90° type on inverted fields:
- Field inversion (which sections flip):
- Generating rule application:
- Switcher arrow (file + referent, or page-scale object):

## Word-as-object responsive behavior

See `design/responsive-object.md`.

- Object text:
- Desktop (≥1024):
- Tablet (600–1023):
- Mobile (320–599):
- Changes orientation by breakpoint: yes / no
- Crops (overflow hidden): yes / no, at which breakpoints

## Motion choreography (Dumbar/DEPT surfaces only)

See `design/motion-choreography.md`. Jetset surfaces: no choreography.

- Entrance order per section:
- Stagger: (default from design-plan.md, override here if needed)
- Trigger: `scroll-into-view` | `page-load` | `user-action`

## Image treatment

See `design/image-treatment.md`. Owner prepares all print grain. Agent places.

- Owner-prepared raster files:
- Print grain applied: halftone / CMYK blow-up / silkscreen / overprint / type-as-dot
- Agent CSS: crop and placement only, no filter simulation

## Collage (Classic Dumbar surfaces only, delete if not used)

See `design/collage.md`. Owner composes, agent implements.

- Layers (order = z-index, bottom to top):
- Owner delivers:
- Blend intent per layer:
- Grid columns occupied:
- Mobile behavior:

## Content sources

- Collection / file:
- Data honesty notes:

## States

Empty, error, loading, offline, reduced-motion.

## Acceptance

- [ ] One dominant intention
- [ ] Packing felt (not drawn); generating rule applied
- [ ] No card soup / no centered marketing stack
- [ ] Word-as-object responsive behavior declared and implemented
- [ ] Motion choreography matches page brief (or Jetset: no choreography)
- [ ] Image grain is owner-prepared, not CSS-simulated
- [ ] WCAG 2.2 AA
- [ ] Keyboard path + `:focus-visible` in a locked chroma
- [ ] No JS island unless this page proves it needs one
