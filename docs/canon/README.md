# Method canon

Not the website. A single rupture fragment so an agent can imitate **structure**, not invent a second aesthetic.

| File | Role |
|---|---|
| `this-is.html` | Open in a browser. Twelve-column grid, oversized crop, one rotation angle, `<dl>` index, functional chevron, `SAMPLE DATA`. UA grotesk. No accent. |
| `this-is.png` | Screenshot of that file. Prefer this if you have vision. |
| `THIS-IS-NOT.md` | Named bans. Also enforced by `npm run fence`. |

When `design-plan.md` is `LOCKED`, rebuild the site with the locked pair, accent, and angle. Do not ship this fragment as a page.
