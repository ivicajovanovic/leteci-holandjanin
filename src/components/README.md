# Components

| Folder | JS | Use |
|---|---|---|
| `primitives/` | No | Button, rule, index, chevron, definition list |
| `sections/` | No | Page sections composed from primitives |
| `islands/` | Yes | Only when HTML/CSS cannot do the interaction. Needs a reason in the spec. |

Do not add files here until `design-plan.md` is `LOCKED`, except shared layout chrome with no identity decisions.
