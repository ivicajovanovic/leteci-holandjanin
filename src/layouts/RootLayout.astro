---
import '../styles/global.css';
import type { IntensityMode } from '../lib/intensity';

interface Props {
  title: string;
  description?: string;
  lang: string;
  skipLabel: string;
  intensity?: IntensityMode;
}

const { title, description, lang, skipLabel, intensity = 'quiet' } = Astro.props;
const hasHeader = Astro.slots.has('header');
const hasFooter = Astro.slots.has('footer');
---

<!doctype html>
<html lang={lang} data-intensity={intensity}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />
    {description && <meta name="description" content={description} />}
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
  </head>
  <body>
    <a class="skip-link" href="#content">{skipLabel}</a>
    {hasHeader && (
      <header>
        <slot name="header" />
      </header>
    )}
    <main id="content">
      <slot />
    </main>
    {hasFooter && (
      <footer>
        <slot name="footer" />
      </footer>
    )}
  </body>
</html>
